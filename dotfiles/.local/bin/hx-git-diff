#!/usr/bin/env sh
set -eu

file=${1:-}

if [ -z "$file" ] || [ ! -e "$file" ]; then
  printf 'No current file to diff\n' >&2
  exit 1
fi

if command -v delta >/dev/null 2>&1; then
  git diff -- "$file" | delta
else
  git diff -- "$file"
fi
