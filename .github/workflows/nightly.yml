name: Nightly

on:
  schedule:
    - cron: "30 2 * * *" # Daily at 02:30 UTC, after the CI nightly run
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

permissions:
  contents: read # Default read-only for all jobs

defaults:
  run:
    shell: bash

env:
  MISE_VERSION: v2026.8.14
  MISE_DATA_DIR: ${{ github.workspace }}/.tools/mise

jobs:
  vendir-status:
    name: Vendir Lock Refresh
    runs-on: ubuntu-26.04
    timeout-minutes: 360

    steps:
      - name: Harden runner
        uses: step-security/harden-runner@05e31511f85b41b11d1cf0ef85d0992719546e2c # v2.21.0
        with:
          egress-policy: block
          allowed-endpoints: >-
            *.blob.core.windows.net:443
            github.com:443
            production.cloudfront.docker.com:443
            *.cloudfront.docker.com:443
            *.github.com:443
            objects.githubusercontent.com:443
            release-assets.githubusercontent.com:443
            mise-versions.jdx.dev:443
            jdx.dev:443
            *.jdx.dev:443
            tuf-repo-cdn.sigstore.dev:443
            *.sigstore.dev:443

      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Setup mise
        uses: jdx/mise-action@c2a87611a18de5b3828c5652fe268e992400cb5c # v4.3.0
        with:
          version: ${{ env.MISE_VERSION }}
          install: false

      - name: Install tools
        run: mise install vendir nushell jq yq

      - name: Add dependency status summary
        run: mise run deps:vendir:summary >>"$GITHUB_STEP_SUMMARY"

  # ==============================================================================
  # Antivirus Gate: Full malware scan
  # ==============================================================================
  antivirus:
    name: Antivirus
    runs-on: ubuntu-26.04
    timeout-minutes: 360

    steps:
      - name: Harden runner
        uses: step-security/harden-runner@05e31511f85b41b11d1cf0ef85d0992719546e2c # v2.21.0
        with:
          egress-policy: block
          allowed-endpoints: >-
            *.blob.core.windows.net:443
            *.cloudflarestorage.com:443
            *.cloudfront.docker.com:443
            *.github.com:443
            *.jdx.dev:443
            *.sigstore.dev:443
            api.osv.dev:443
            auth.docker.io:443
            crates.io:443
            database.clamav.net:443
            files.pythonhosted.org:443
            github.com:443
            jdx.dev:443
            mise-versions.jdx.dev:443
            nodejs.org:443
            objects.githubusercontent.com:443
            production.cloudflare.docker.com:443
            pypi.org:443
            registry-1.docker.io:443
            registry.npmjs.org:443
            release-assets.githubusercontent.com:443

      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Setup mise
        uses: jdx/mise-action@c2a87611a18de5b3828c5652fe268e992400cb5c # v4.3.0
        with:
          version: ${{ env.MISE_VERSION }}
          install: false

      - name: Install the full toolset so the scan has something to look at
        run: mise install

      - name: Update ClamAV database and scan everything
        run: mise run antivirus:all

  # ==============================================================================
  # Security Gate: Container Images Scan
  # ==============================================================================
  images:
    name: Container Images Scan
    runs-on: ubuntu-26.04
    timeout-minutes: 360

    steps:
      - name: Harden runner
        uses: step-security/harden-runner@05e31511f85b41b11d1cf0ef85d0992719546e2c # v2.21.0
        with:
          egress-policy: block
          allowed-endpoints: >-
            *.blob.core.windows.net:443
            *.cloudflarestorage.com:443
            *.cloudfront.docker.com:443
            *.github.com:443
            *.jdx.dev:443
            *.sigstore.dev:443
            auth.docker.io:443
            ghcr.io:443
            github.com:443
            grype.anchore.io:443
            jdx.dev:443
            mise-versions.jdx.dev:443
            objects.githubusercontent.com:443
            pkg-containers.githubusercontent.com:443
            production.cloudflare.docker.com:443
            registry-1.docker.io:443
            release-assets.githubusercontent.com:443
            toolbox-data.anchore.io:443

      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Setup mise
        uses: jdx/mise-action@c2a87611a18de5b3828c5652fe268e992400cb5c # v4.3.0
        with:
          version: ${{ env.MISE_VERSION }}
          install: false

      - name: Install tools
        run: mise install grype

      - name: Scan the pinned container images
        run: mise run security:grype:images

  # ==============================================================================
  # Security Gate: OpenSSF Scorecard
  # ==============================================================================
  scorecard:
    name: OpenSSF Scorecard
    runs-on: ubuntu-26.04
    timeout-minutes: 360

    permissions:
      contents: read
      actions: read # Required by Scorecard to read workflow run data.
      id-token: write # Required to publish the signed results to the public dataset.
      security-events: write # Required to upload the SARIF report to the GitHub Security tab.

    steps:
      - name: Harden runner
        uses: step-security/harden-runner@05e31511f85b41b11d1cf0ef85d0992719546e2c # v2.21.0
        with:
          egress-policy: block
          allowed-endpoints: >-
            *.sigstore.dev:443
            api.deps.dev:443
            api.github.com:443
            api.osv.dev:443
            api.securityscorecards.dev:443
            bestpractices.coreinfrastructure.org:443
            github.com:443
            oss-fuzz-build-logs.storage.googleapis.com:443
            sigstore-tuf-root.storage.googleapis.com:443
            uploads.github.com:443
            www.bestpractices.dev:443

      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Run analysis
        uses: ossf/scorecard-action@2d1146689b8cda280b9bc96326124645441f03bc # v2.4.4
        with:
          results_file: scorecard-results.sarif
          results_format: sarif
          publish_results: true

      - name: Upload Scorecard SARIF
        uses: github/codeql-action/upload-sarif@db488ddef3bf6cb639b32c2e9a7c0a7ea8271d28 # v4.37.8
        with:
          sarif_file: scorecard-results.sarif
